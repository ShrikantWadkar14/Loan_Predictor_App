services:
  - type: web
    name: loan-predictor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    plan: free
    envVars:
      - key: RUNTIME_VERSION
        value: python-3.10.13
